module.exports = (function () {
    // var baseUrl = 'http://ericcarraway.com';
    var baseUrl = 'http://127.0.0.1:4000/';

    return {
        baseUrl: baseUrl
    };
}());

// #TODO - easy switching between prod and dev via CLI
